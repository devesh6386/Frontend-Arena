import './globals.css'
export const metadata={title:'After the Song — Life in Receipts',description:'A living archive of connected moments'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
