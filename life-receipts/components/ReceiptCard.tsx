'use client'
import {Receipt} from '@/types'; import {Music2, ShoppingBag, Clock3, ArrowUpRight} from 'lucide-react'
export default function ReceiptCard({r,active,onClick}:{r:Receipt;active?:boolean;onClick?:()=>void}){const music=r.type==='music';return <button onClick={onClick} className={`text-left w-full group border-b border-[#d9d1c2] py-4 transition ${active?'bg-[#ebe4d7] -mx-3 px-3':''}`}><div className="flex gap-3"><div className={`mt-1 h-8 w-8 rounded-full grid place-items-center ${music?'bg-[#dfe8ee] text-[#47738e]':'bg-[#f5dfd6] text-[#bd5839]'}`}>{music?<Music2 size={15}/>:<ShoppingBag size={15}/>}</div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><span className="text-[10px] uppercase tracking-[.18em] text-[#777064]">{music?'music':'purchase'} · {new Date(r.timestamp).toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'})}</span><ArrowUpRight size={14} className="opacity-0 group-hover:opacity-100"/></div><p className="font-semibold truncate mt-1">{r.title}</p><p className="text-xs text-[#756e63] truncate">{r.subtitle}</p><div className="flex gap-3 text-[11px] text-[#8a8071] mt-2">{music?<><span>{r.duration||0} min played</span><span>{new Date(r.timestamp).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}</span></>:<><span>{r.amount!==undefined?`₹${r.amount.toLocaleString('en-IN')}`:''}</span><span>{r.category}</span></>}</div></div></div></button>}
